# CLAUDE.md

This is a sample Python project for agentkit smoke testing.

## Structure
- `src/main.py` — core functions
- `tests/test_main.py` — unit tests

## Commands
- Run tests: `pytest`
- Run linting: `ruff check .`
