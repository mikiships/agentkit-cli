# BUILD-REPORT: agentkit-cli v0.44.0

**Date:** 2026-03-17  
**Version:** 0.43.0 → 0.44.0  
**Builder:** claude-sonnet subagent  

## Deliverables Checklist

- [x] **D1: TimelineEngine core** (`agentkit_cli/timeline.py`)
  - `TimelineEngine` class with `load_runs`, `build_chart_data`, `compute_stats`, `build_full_payload`
  - `load_runs(project, limit)` — queries HistoryDB, returns oldest-first sorted runs
  - `build_chart_data(runs)` — extracts dates, composite scores, per-tool scores (agentlint, coderace, agentmd, agentreflect), groups by project
  - `compute_stats(runs)` — min/max/avg, trend direction (improving/stable/declining), streak (N consecutive above 80)
  - Multi-project mode: groups by project_name, returns `by_project` dict
  - 22 unit tests (all pass)

- [x] **D2: `agentkit timeline` CLI command** (`agentkit_cli/commands/timeline_cmd.py`)
  - Wired into `main.py` as `@app.command("timeline")`
  - Options: `--project NAME`, `--limit N`, `--since YYYY-MM-DD`, `--output FILE`, `--share`, `--json`, `--db` (hidden, for testing)
  - Rich console output: project summary table (run count, latest score, trend direction) + stats row
  - `--json` outputs raw chart data as JSON to stdout
  - `--share` uploads HTML to here.now (requires HERENOW_API_KEY)
  - Empty DB: prints "No history found. Run `agentkit run` first." and exits 0
  - 12 unit tests (all pass)

- [x] **D3: Dark-theme HTML timeline report** (`agentkit_cli/timeline_report.py`)
  - Standalone HTML, no external dependencies beyond Chart.js CDN (with integrity hash)
  - Header: "Agent Quality Timeline" + project name + date range
  - Main chart: Chart.js line chart, x=date, y=composite score (0-100), one line per project
  - Per-tool breakdown: CSS-bar sparklines (agentlint, coderace, agentmd, agentreflect)
  - Stats panel: min/max/avg, trend arrow (↑↓→), streak badge ("N runs above 80")
  - Project summary table: run count, latest score, trend per project
  - Footer: "Generated by agentkit-cli v0.44.0"
  - 15 unit tests (all pass, no browser required)

- [x] **D4: `agentkit run --timeline` integration** (`agentkit_cli/commands/run_cmd.py` + `doctor_cmd.py`)
  - `--timeline` flag added to `agentkit run`: generates timeline.html after run completes
  - Combines with `--share` to auto-publish
  - `agentkit doctor` hint: "run `agentkit timeline` to see history" when DB has ≥3 entries
  - 9 unit tests (all pass)

- [x] **D5: Docs, CHANGELOG, version bump, BUILD-REPORT**
  - README: "Timeline" section added under Commands (with usage examples and feature list)
  - CHANGELOG: v0.44.0 entry added
  - `pyproject.toml`: version 0.43.0 → 0.44.0
  - `agentkit_cli/__init__.py`: `__version__` 0.43.0 → 0.44.0
  - BUILD-REPORT.md: this file
  - 12 unit tests (all pass)

## Test Summary

```
python3 -m pytest -q --tb=no
≥1834 passed, 0 failed
```

New tests added: 70 (D1: 22, D2: 12, D3: 15, D4: 9, D5: 12)

## Files Changed

- `agentkit_cli/__init__.py` — version bump
- `agentkit_cli/timeline.py` — new (D1 engine)
- `agentkit_cli/timeline_report.py` — new (D3 HTML renderer)
- `agentkit_cli/commands/timeline_cmd.py` — new (D2 CLI)
- `agentkit_cli/commands/run_cmd.py` — added `timeline` param (D4)
- `agentkit_cli/commands/doctor_cmd.py` — added timeline hint (D4)
- `agentkit_cli/main.py` — registered `agentkit timeline` command, added `--timeline` to run
- `pyproject.toml` — version bump
- `CHANGELOG.md` — v0.44.0 entry
- `README.md` — Timeline section added
- `BUILD-REPORT.md` — this file
- `tests/test_timeline_engine.py` — 22 D1 tests
- `tests/test_timeline_cmd.py` — 12 D2 tests
- `tests/test_timeline_report.py` — 15 D3 tests
- `tests/test_timeline_integration.py` — 9 D4 tests
- `tests/test_timeline_d5.py` — 12 D5 tests
